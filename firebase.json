{
  "database": {
    "rules": "database.rules.json"
  },
  "firestore": {
    
  },
  "functions": {
    "predeploy": "npm --prefix \"%RESOURCE_DIR%\" run build"
  },
  "hosting": {
    "public": "public",
    "rewrites": [
      {
        "source": "**",
        "destination": "/index.html"
      }
    ]
  },
  "storage": {
    "rules": "storage.rules"
  }
}